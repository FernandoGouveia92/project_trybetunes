import React from 'react';
import LoadingText from '../styles/loading/styles';

class Loading extends React.Component {
  render() {
    return (
      <LoadingText>Carregando...</LoadingText>
    );
  }
}

export default Loading;
